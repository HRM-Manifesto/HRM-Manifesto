# HRM Core 1.0.0 — Signed Founding Release

Founding author: Aleksander Krzymowski
Doctrine version: HRM Version 1.0, dated 30 August 2026.

This release is a preservation package for the existing HRM founding corpus. It does not introduce a new doctrine.

Verification:
- verify `release.json.minisig` against `HRM_FOUNDER.pub` using Minisign;
- verify every payload SHA-256 declared in `release.json`;
- the transport ZIP SHA-256 is published as a separate `.sha256` release asset; identity/provenance is anchored by the signed `release.json` and its payload hashes.

Founder Minisign public key ID: `9C609B0EBE5470DB`.

Language status:
- Polish: original;
- English: canonical translation;
- Swedish: additional official translation.